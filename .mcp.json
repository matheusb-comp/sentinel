{
  "mcpServers": {
    "laravel-boost": {
      "command": "docker",
      "args": [
        "compose",
        "exec",
        "-T",
        "api",
        "php",
        "artisan",
        "boost:mcp"
      ]
    }
  }
}
